bin_PROGRAMS = vdens
man_MANS = vdens.1
CFLAGS = --pedantic -Wall -Wextra 
# uncomment to enable further checks on sources
#CFLAGS = --pedantic -Wall -Wextra -O2 -D_FORTIFY_SOURCE=2

vdens_SOURCES = vdens.c
vdens_LDADD = -lcap -lexecs -lvdeplug

